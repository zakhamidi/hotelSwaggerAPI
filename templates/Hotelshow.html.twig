{% extends 'base.html.twig' %}

{% block title %}Hello Hotel!{% endblock %}
{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello Hotel! ✅</h1>
    {% for hotel in hotels %}
    <h2>your Hotel is {{hotel.name}}</h2><span>with the category: {{hotel.category}}</span>
    <p><img src="{{hotel.image}}"></p>
    <h2>Rating</h2>{% for location in hotel.locations %}
    <h3>{{location.city}} || {{location.state}} || {{location.country}} || {{location.zipcode}} </h3>{% endfor %}
    <p>
        <form action="">
  <p>Rate the Hotel :</p>
  <div>
    <input type="radio" id="contactChoice1"
     name="contact" value="0">
    <label for="contactChoice1">0</label>

    <input type="radio" id="contactChoice1"
     name="contact" value="1">
    <label for="contactChoice1">1</label>

    <input type="radio" id="contactChoice1"
     name="contact" value="2">
    <label for="contactChoice1">2</label>

    <input type="radio" id="contactChoice1"
     name="contact" value="3">
    <label for="contactChoice1">3</label>

    <input type="radio" id="contactChoice1"
     name="contact" value="04>
    <label for="contactChoice1">4</label>

    <input type="radio" id="contactChoice1"
     name="contact" value="5">
    <label for="contactChoice1">5</label>
  </div>
  <div>
    <button type="submit">Envoyer</button>
  </div>
</form>
</p>
<p><a href={{path('Hotelform')}}>you can add new Hotel here</a></p>
<p><a href={{path('Hotelform',{'id': hotel.id})}}>Modify your hotel here</a></p>
{% endfor %}
{% endblock %}
